# 日程管理系统技术方案

## 1. 项目概述

### 1.1 项目目标

构建一套面向企业的多租户日程管理系统，覆盖 PC 端和移动端（响应式 Web），提供日程管理、第三方系统集成（会议系统、差旅系统等）以及 AI 辅助能力，部署于企业内部私有云。

### 1.2 核心角色

| 角色 | 说明 |
|------|------|
| 系统管理员 | 管理租户、组织架构、权限模板、全局配置 |
| 租户管理员 | 管理本租户的组织架构、角色、用户、第三方集成 |
| 普通用户 | 使用日程管理功能、查看第三方推送日程、使用 AI 能力 |
| 第三方系统 | 通过 API 推送事件到指定用户的日程 |

### 1.3 技术栈概览

| 层级 | 技术选型 |
|------|----------|
| 前端框架 | Vue 3 + TypeScript |
| UI 组件库 | Naive UI（PC）/ Vant（Mobile），统一响应式适配 |
| 状态管理 | Pinia |
| 路由 | Vue Router |
| 构建工具 | Vite |
| 后端框架 | NestJS（Node.js） |
| 数据库 | PostgreSQL（主） + Redis（缓存 & 会话） |
| 消息队列 | RabbitMQ（异步事件处理） |
| 搜索引擎 | Elasticsearch（可选，用于日历全文检索） |
| AI 服务 | 对接企业内部大模型或 OpenAI 兼容接口 |
| ASR 语音识别 | Web Speech API / 科大讯飞 / 阿里云语音识别（企业内部私有化部署） |
| 容器化 | Docker + Docker Compose / Kubernetes |
| CI/CD | GitLab CI / Jenkins |
| 监控 | Prometheus + Grafana + ELK |

## 2. 整体架构

### 2.1 架构分层

```
+--------------------------------------------------------------------+
|                    前端（Vue 3 SPA）                               |
|  PC 端: 完整工作台视图 -- 移动端: 触屏优化视图 -- PWA              |
+----------------------------------+---------------------------------+
                                   | HTTPS
+----------------------------------+---------------------------------+
|                      网关层 (API Gateway)                          |
|       速率限制 | 认证鉴权 | 请求路由 | 数据校验                     |
+----------------------------------+---------------------------------+
                                   |
+----------------------------------+---------------------------------+
|                      应用服务层 (NestJS)                           |
|                                                                     |
|  +----------+ +----------+ +----------+ +-----------+ +--------+  |
|  | 用户服务  | | 日程服务  | | 通知服务  | | 第三方集成 | | AI服务 |  |
|  | Auth     | | Calendar | | Notify   | | 网关服务   | | AI Svc |  |
|  +----------+ +----------+ +----------+ +-----------+ +--------+  |
|                                                                     |
|  +----------+ +----------+ +----------+                            |
|  | 组织服务  | | 权限服务  | | 租户服务  |                            |
|  | Org      | | RBAC    | | Tenant   |                            |
|  +----------+ +----------+ +----------+                            |
+----------------------------------+---------------------------------+
                                   |
+----------------------------------+---------------------------------+
|                      基础设施层                                     |
|  PostgreSQL | Redis | RabbitMQ | Elasticsearch | 对象存储 (MinIO)  |
+--------------------------------------------------------------------+
```

### 2.2 外部系统集成架构

```
+-------------------+     +-------------------+     +------------------+
|    会议系统        |     |    差旅系统        |     |  其他第三方系统  |
| (Zoom/Teams/内部)  |     | (携程商旅/内部)    |     |                  |
+--------+----------+     +--------+----------+     +--------+---------+
         |  API Key               |  API Key               |  API Key
         v                        v                        v
+--------------------------------------------------------------------+
|                   第三方集成 API 网关                               |
|              HTTPS + IP 白名单 + API Key 鉴权                       |
|          /api/v1/external/events - 推送日程事件                     |
|          /api/v1/external/resources - 推送资源/人员                 |
+----------------------------------+---------------------------------+
                                   |
         +-------------------------+----------------------------+
         v                         v                            v
  事件校验 & 标准化           去重 & 冲突检测              持久化到数据库
         |                         |                            |
         +-------------------------+----------------------------+
                                   v
                        RabbitMQ 异步通知
                                   |
                    +--------------+-------------+
                    v              v             v
               WebSocket       邮件/消息      AI 智能分析
               实时推送         推送通知       (冲突/建议)
```

## 3. 功能模块详述

### 3.1 租户与组织架构管理

- **多租户模型**：数据按租户隔离（schema 级或 tenant_id 行级）
- **组织架构**：支持树形结构（公司 -> 部门 -> 小组），支持层级深度配置
- **用户管理**：批量导入/导出，支持 CSV、LDAP 同步
- **角色权限**：基于 RBAC 模型，支持预置角色 + 自定义角色

| 预置角色 | 权限范围 |
|----------|----------|
| 超级管理员 | 全局管理，跨租户操作 |
| 租户管理员 | 租户内全部管理权限 |
| 部门经理 | 查看本部门所有成员日程（只读），审批权限 |
| 普通员工 | 管理自己日程，查看公开日程 |
| 第三方应用 | 通过 API Key 推送事件到指定用户 |

### 3.2 日程管理核心功能

**视图模式**
- 日视图、周视图、月视图、列表视图
- 日程时间线（甘特图风格）
- 多维度筛选（按类型、按项目、按人）

**日程事件**
- 标题、描述、地点（支持地图）、时间（起止/全天）、重复规则（RRule 标准）
- 类型标签（会议 / 差旅 / 个人 / 任务）
- 优先级、提醒设置（支持多级提醒）
- 附件（支持上传图片、文档）
- 参与者管理（可添加内部用户、外部联系人）
- 评论与回复（支持 @ 提醒）

**共享与协作**
- 日程分享（生成外部链接，支持密码/时效限制）
- 团队日历（查看部门/项目组日程）
- 忙闲状态查询（Busy/Free 接口）

**冲突检测**
- 创建/更新事件时自动检测时间冲突
- 冲突弹窗提示，支持强制创建或调整时间

### 3.3 第三方集成引擎

**API 设计**

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/external/events | POST | 推送单个日程事件 |
| /api/v1/external/events/batch | POST | 批量推送日程事件 |
| /api/v1/external/events/{id} | PUT | 更新已推送的日程 |
| /api/v1/external/events/{id} | DELETE | 删除已推送的日程 |
| /api/v1/external/events/sync | POST | 全量同步（覆盖式） |
| /api/v1/external/event-types | POST | 注册自定义事件类型 |
| /api/v1/external/health | GET | 健康检查 |

**API Key 管理**
- 每个第三方系统分配独立的 API Key
- 支持多 Key 轮换、过期时间、IP 白名单
- API Key 绑定事件类型白名单（如会议系统的 Key 只能推送"会议"类型）
- 调用次数配额管理
- 调用日志与审计

**事件标准化模型**

```json
{
  "event_id": "ext-20260719-001",
  "source": "meeting-system",
  "event_type": "meeting",
  "summary": "Q3 产品路线图评审",
  "description": "评审下半年产品规划...",
  "start_time": "2026-07-20T09:00:00+08:00",
  "end_time": "2026-07-20T11:00:00+08:00",
  "timezone": "Asia/Shanghai",
  "location": {
    "name": "3F 大会议室",
    "address": "",
    "coordinates": null
  },
  "organizer": {
    "user_id": "user-001",
    "name": "张三"
  },
  "attendees": [
    {"user_id": "user-002", "name": "李四", "required": true},
    {"user_id": "user-003", "name": "王五", "required": false}
  ],
  "recurrence": null,
  "attachments": [],
  "reminders": [15, 60],
  "visibility": "public",
  "metadata": {
    "meeting_room": "3F-大会议室",
    "meeting_id": "mtg-20260719-001",
    "recording_url": null
  }
}
```

**推送流程**
1. 第三方系统通过 API Key 鉴权调用推送接口
2. 服务端校验事件数据合法性（必填字段、时间格式、用户存在性）
3. 将第三方事件 ID 与系统事件关联，实现幂等更新
4. 检测时间冲突，记录冲突日志并在用户端标记
5. 通过 WebSocket 实时推送更新到前端
6. 触发用户设置的提醒

### 3.4 通知与提醒

| 通知渠道 | 实现方式 |
|----------|----------|
| 站内实时通知 | WebSocket + 消息中心 |
| 邮件通知 | SMTP（支持模板定制） |
| 企业微信/钉钉/飞书 | Bot Webhook 推送 |
| 浏览器推送 | Web Push API |
| 移动端推送 | 随 Web App 内 Notification API 或 PWA |

### 3.5 AI 智能功能

**3.5.1 语音创建日程（核心入口）**

语音输入作为 AI 创建日程的首选交互方式，用户在任意页面点击麦克风按钮开始语音输入，系统自动完成语音识别（ASR）到语义解析的完整链路：

**操作流程**

```
用户点击麦克风 -> 语音录制 -> ASR 语音转文字 -> LLM 语义解析 -> 结构化日程预览 -> 用户确认 -> 创建完成
```

**示例场景**

> 用户说出："下周三下午2点到4点跟产品团队开需求评审会，地点在3楼大会议室"
> 系统输出：结构化日程事件预览（标题、时间、地点、参与者、描述），用户确认后一键创建

**交互方式**

- 移动端：底部导航栏常驻"语音+"按钮，支持一键开始语音录入
- PC 端：日程创建页面的麦克风图标按钮，点击开始语音输入
- 支持连续对话：用户说"把刚才那个会改到周五下午"，AI 结合上下文执行修改
- 支持快捷指令："创建一个明天的全天日程：休假" -> 直接创建全天休假日程
- 支持多轮修正：用户确认预览后说"把地点改到2楼会议室"，系统即时更新

**技术实现**

- 前端使用 Web Speech API（移动端）或 ASR SDK（PC 端）进行录音和初步语音识别
- 企业内网部署专用 ASR 引擎，语音数据不出内网
- ASR 结果送入 LLM（通过 Function Calling 约束输出结构），实现端到端语义理解
- 解析结果以日程卡片形式呈现，用户确认后再写入数据库

**3.5.2 智能冲突与建议**

- 基于历史日程分析用户的忙碌时段模式
- 建议最佳空闲时间段
- 检测跨时区会议的最佳时间
- 提醒过长会议建议拆分

**3.5.3 日程摘要与周报**

- 自动生成本日/本周/本月日程摘要
- 按项目/类型统计时间投入
- 生成周报草稿（含已完成的会议、差旅等）

**3.5.4 智能提醒**

- 根据用户习惯推断提醒时间
- 出行时间提醒（如会议在外部地点，推送建议出发时间）


**AI 服务架构**

```
+----------+   +---------+   +----------+   +----------+   +----------+
| 语音输入  |-> | ASR 语音 |-> | LLM 语义 |-> | 结构化   |-> | 用户确认  |-> 创建日程
| (麦克风)  |   | 转文字   |   | 解析     |   | 日程预览  |   | (一键确认)|
+----------+   +---------+   +----------+   +----------+   +----------+
                                     |
                            +---------+---------+
                            |  Prompt 模板库    |
                            |（Function Calling |
                            |  + Few-shot 示例） |
                            +------------------+

注：也支持文字输入作为备选方案，流程为 文字输入 -> LLM 语义解析 -> ...
```

- 通过 Prompt 模板 + Function Calling 实现结构化解析
- 支持企业内部大模型 API（兼容 OpenAI 格式即可）
- 敏感信息隔离：不向模型发送超过必要范围的数据
- 支持用户反馈（"解析正确" / "解析有误"）用于持续优化

## 4. 数据库设计概要

### 4.1 核心表结构

```
tenants                  -- 租户表
|-- id, name, domain, plan, status, created_at
|-- saml_config          -- SAML SSO 配置（JSONB）
|-- features             -- 功能开通状态（JSONB）

organizations            -- 组织架构表（树形结构，adjacency list）
|-- id, tenant_id, parent_id, name, level, sort_order
|-- manager_id           -- 部门负责人

users                    -- 用户表
|-- id, tenant_id, org_id, name, email, phone
|-- saml_id, status
|-- preferences          -- 用户偏好（JSONB，时区、提醒默认值等）

roles                    -- 角色表
|-- id, tenant_id, name, permissions（JSONB）
|-- is_system            -- 是否为系统预置角色

user_roles               -- 用户角色关联表
|-- user_id, role_id, scope_type, scope_id

events                   -- 日程事件表
|-- id, tenant_id, user_id, summary, description
|-- start_time, end_time, timezone, all_day
|-- event_type, priority, status
|-- location（JSONB）, recurrence（RRule）
|-- ext_source, ext_event_id     -- 第三方来源标识（唯一联合索引）
|-- reminder_config（JSONB）
|-- visibility, created_by, updated_at

event_attendees          -- 参与者表
|-- event_id, user_id, required, status, responded_at
|-- ext_attendee_name    -- 外部参与者姓名

event_comments           -- 日程评论
|-- event_id, user_id, content, created_at

third_party_apps         -- 第三方应用注册
|-- tenant_id, app_name, api_key_hash, ip_whitelist
|-- allowed_event_types, rate_limit, expires_at
|-- status, created_by

api_audit_log            -- API 调用审计
|-- app_id, api_key_id, event_type, request_summary
|-- ip_address, result, created_at

notifications            -- 通知表
|-- user_id, type, title, content
|-- is_read, related_event_id
|-- created_at
```

### 4.2 数据隔离策略

- **多租户隔离**：使用 tenant_id 行级隔离，所有核心表带 tenant_id 字段
- **数据库层**：PostgreSQL RLS（Row Level Security）保障租户间数据严格隔离
- **缓存隔离**：Redis Key 使用 {tenant_id}: 前缀
- **第三方 API 隔离**：API Key 归属具体租户，只能操作所属租户的数据

## 5. 前端设计概要

### 5.1 响应式策略

使用 CSS Media Queries + 容器查询实现一套代码适配 PC 和移动端。

| 维度 | PC 端（>1024px） | 平板端（768-1024px） | 移动端（<768px） |
|------|-----------------|---------------------|-----------------|
| 布局 | 多列布局，侧边栏常驻 | 两列或简化布局 | 单列，底部 Tab 导航 |
| 视图切换 | 日历 + 列表并排 | 日历为主，列表可切换 | 全屏日历，滑动切换 |
| 操作方式 | 鼠标拖拽、右键菜单 | 触控 + 简化操作 | 全触控，底部弹出操作 |

### 5.2 核心页面

| 页面 | 功能 |
|------|------|
| 登录页 | SAML SSO 登录、账号密码登录 |
| 日程首页 | 日历视图（日/周/月）、日程列表 |
| 日程详情 | 查看/编辑/删除日程，参与者管理 |
| 新建日程 | 表单创建 + AI 自然语言创建入口 |
| 团队日历 | 查看团队成员日程（权限控制） |
| 消息中心 | 通知列表、审批请求 |
| 设置页 | 个人偏好、时区、通知设置 |
| 管理后台 | 租户管理、组织管理、角色权限、第三方集成管理 |
| API Key 管理 | 创建/撤销 Key、查看调用统计 |
| 个人中心 | 个人信息、绑定第三方账号 |

### 5.3 移动端体验优化

- 底部导航栏（日程 / 消息 / AI+ / 日历 / 我的）
- 全屏触控手势：左右滑动切换日期，长按拖拽调整时间
- PWA 支持：可添加到桌面，离线查看已缓存日程
- 下拉刷新同步最新数据
- 轻量视图：移动端聚焦于当天日程，减少信息密度
- 语音优先交互：底部导航栏常驻语音按钮，全场景支持语音创建日程

## 6. 安全设计

| 安全项 | 方案 |
|--------|------|
| SAML SSO | 支持标准 SAML 2.0 协议，对接企业 IdP（Azure AD / Okta / Keycloak） |
| API 鉴权 | API Key + HMAC 签名，HTTPS 传输 |
| 数据加密 | 传输层 TLS，敏感字段（API Key 等）AES-256 加密存储 |
| 防篡改 | API 请求带时间戳 + 签名，防止重放攻击 |
| 速率限制 | 基于 Redis 的滑动窗口限流，按租户 + API Key 维度 |
| 审计日志 | 所有第三方 API 调用记录完整审计日志 |
| XSS/CSRF | 前端 Content Security Policy，CSRF Token |
| SQL 注入 | ORM 参数化查询 + 输入校验 |

## 7. 部署架构

```
+--------------------- 企业内部私有云 ---------------------+
|                                                            |
|  +-------------+  +-------------+  +-------------+        |
|  |  Nginx/     |  |  Nginx/     |  |  Nginx/     |        |
|  |  K8s Ingress |  |  K8s Ingress|  |  K8s Ingress|        |
|  +------+------+  +------+------+  +------+------+        |
|         |                 |                |                |
|  +------+------+  +------+------+  +------+------+        |
|  | 前端服务     |  | 后端服务     |  | 后端服务     |        |
|  | (Vue SPA)   |  | (NestJS)   |  | (NestJS)   |        |
|  | Replicas: 2 |  | Replicas: 3|  | Replicas: 3|        |
|  +-------------+  +-------------+  +-------------+        |
|                                                            |
|  +-------------+  +-------------+  +---------------+       |
|  | PostgreSQL  |  |   Redis     |  |  RabbitMQ     |       |
|  | Master+Slave|  |   Cluster   |  |  Cluster      |       |
|  +-------------+  +-------------+  +---------------+       |
|                                                            |
|  +-------------+  +-------------+                         |
|  |   MinIO     |  |   ELK       |                         |
|  | (对象存储)   |  | (日志)      |                         |
|  +-------------+  +-------------+                         |
|                                                            |
|  +-------------+  +-------------+                         |
|  | Prometheus  |  |  GitLab    |                         |
|  | + Grafana   |  |  CI/CD     |                         |
|  +-------------+  +-------------+                         |
+------------------------------------------------------------+
```

## 8. 实施路线图

| 阶段 | 内容 | 预计周期 |
|------|------|----------|
| P0 - 基础框架 | 项目脚手架、数据库初始化、租户/用户/组织模块、SAML SSO 集成 | 3-4 周 |
| P1 - 日程核心 | 日程 CRUD、日历视图、重复规则、参与者管理、提醒 | 4-5 周 |
| P2 - 第三方集成 | API 网关、API Key 管理、事件推送接口、幂等处理、审计日志 | 2-3 周 |
| P3 - 协作与通知 | 团队日历、日程分享、审批、WebSocket 实时推送、消息通知 | 2-3 周 |
| P4 - AI 功能 | 自然语言创建日程、智能冲突建议、日程摘要 | 3-4 周 |
| P5 - 优化与收尾 | 移动端体验打磨、PWA、性能优化、监控、文档 | 2 周 |

## 9. 关键设计决策与说明

### 9.1 为什么选择 NestJS？

- 内置模块化架构（Module / Controller / Service / Provider），天然支持微服务拆分
- 装饰器语法适合复杂的企业级业务，如参数校验、鉴权、日志等可通过装饰器统一管理
- TypeORM / Prisma ORM 原生支持 PostgreSQL 的多租户策略
- 完善的测试工具链（Jest + Supertest）

### 9.2 为什么选择 PostgreSQL RLS 做租户隔离？

- 相比 schema 隔离，行级隔离的运维成本更低（不需要动态创建 schema）
- 适合租户数量多但单个租户数据量适中的场景
- 应用层 + 数据库层双重保障，数据安全更可靠

### 9.3 响应式方案选型说明

采用同一套 Vue 3 代码库，通过 CSS 和组件级适配实现响应式，不做 PC 和移动端分别部署：

- **优势**：统一维护、快速迭代、用户跨端体验一致
- **权衡**：纯移动端体验略逊于原生 App，但通过 PWA + 离线缓存可弥补大部分差距

### 9.4 第三方推送事件的幂等设计

- 使用 (ext_source, ext_event_id) 联合唯一索引保证同一条外部事件不会重复创建
- 第三方更新事件时使用 ext_source + ext_event_id 定位，支持 PUT 幂等更新
- 重复推送同一事件时直接返回已有事件的 ID，不产生重复数据

## 10. 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| SAML 集成复杂度高 | 认证流程卡顿 | 先用本地账号体系，SAML 二期接入；选中 Keycloak 做桥接 |
| 第三方系统数据质量差 | 日程出现乱码/错位 | API 入口做严格校验，不合格数据返回 422 + 错误详情 |
| 大量日程推送导致性能瓶颈 | 响应变慢 | 批量接口、异步处理（RabbitMQ）、读写分离 |
| AI 解析准确率不足 | 用户信任度下降 | 解析结果始终让用户确认后才创建，逐步收集反馈微调 |
| 语音识别（ASR）准确率受环境噪声影响 | 关键日程信息识别错误 | 提供静音/降噪提示；支持识别结果手动编辑修正；针对专有名词提供热词库配置 |
| 企业内部大模型接口不稳定 | AI 功能不可用 | 设计多 Provider 切换机制，支持降级为纯表单创建 |

---

*本方案为初版技术方案，各模块细节可在确认后进一步展开。*

